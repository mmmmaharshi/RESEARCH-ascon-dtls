# Agent / editor state
.opencode/

# Build artifacts
build/
build-asan/
out/
*.o
*.obj
*.a
*.elf
*.exe
*.lib
*.dll

# Test / CLI logs
cli_*.txt
srv_*.txt
gdb_out.txt

# OS / IDE noise
.DS_Store
Thumbs.db

# Vendored dependency: wolfSSL is its own git repository (holds the Ascon-DTLS
# fork). Not vendored here; kept as a sibling checkout.
wolfssl/

# Coq build artifacts (mask_prf.v is the source — keep it tracked)
tools/coq/*.vo
tools/coq/*.vok
tools/coq/*.vos
tools/coq/*.glob
tools/coq/.lia.cache
tools/coq/.mask_prf.aux

tools/coq/.*.aux
tools/easycrypt/*.eco

# Evidence logs are intentionally tracked (reproducibility): do NOT ignore
# tools/easycrypt/mask_prf.compile.log, tools/coq/*.compile.log, tools/ascon_mask_dudect.log
# Repro: easycrypt compile mask_prf.ec ; coqc mask_prf.v ; ./tools/ascon_mask_dudect.exe
